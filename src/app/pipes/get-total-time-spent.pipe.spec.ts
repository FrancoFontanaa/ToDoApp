import { GetTotalTimeSpentPipe } from './get-total-time-spent.pipe';

describe('GetTotalTimeSpentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTotalTimeSpentPipe();
    expect(pipe).toBeTruthy();
  });
});
